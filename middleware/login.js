export default function ({ store, redirect, route }) {
/*  if (process.client && route.name !== 'login') {
    if (!store.$auth.$state.loggedIn || !store.$auth.$state.user || store.$auth.$state.user.user_type !== 'Admin') {
      return redirect('/login')
    }
  } */
}
